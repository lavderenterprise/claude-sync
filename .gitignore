# Build output
ClaudeSessionSync.app/
*.dSYM/
.build/

# macOS
.DS_Store

# Icon build intermediate (regenerated by Icon/make_icon.py; AppIcon.icns is committed)
Icon/AppIcon.iconset/

# Backups produced by the tool itself (never inside the repo, but just in case)
claude-code-sessions.backup-*/
